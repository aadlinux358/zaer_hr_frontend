export enum CRUDType {
  READ = 'READ',
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE'
}

export enum DownloadFileType {
  CSV = 'csv',
  EXCEL = 'xlsx',
  PDF = 'pdf',
}

export enum AppLanguage {
  ENG = 'ENG',
  ITA = 'ITA',
  TIG = 'TIR'
}
